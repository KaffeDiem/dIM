import XCTest
@testable import CryptoController

final class CryptoControllerTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(CryptoController().text, "Hello, World!")
    }
}
